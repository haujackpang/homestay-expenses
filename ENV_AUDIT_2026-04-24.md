# Environment Audit 2026-04-24

## Scope
- Canonical test repo: `haujackpang/homestayERP-test`
- Canonical live repo: `haujackpang/homestay-expenses`
- Test Supabase project: `skwogboredsczcyhlqgn`
- Live Supabase project: `afcifzghlkxvnpulahub`

## Verified Current Site Mapping
- Test site currently points to `https://skwogboredsczcyhlqgn.supabase.co`
- Live site currently points to `https://afcifzghlkxvnpulahub.supabase.co`

## Profiles Audit
- Test `profiles`: 4 records
- Live `profiles`: 4 records
- The visible profile records match exactly by:
  - `id`
  - `email`
  - `full_name`
  - `role`
  - `active`
  - `created_at`

Matching profile emails:
- `admin@homestay.app`
- `azizul@homestay.app`
- `jack@homestay.app`
- `jocelyn@homestay.app`

## Claims Audit
- Test `claims`: 31 records
- Live `claims`: 31 records
- For the common claim fields queried from both environments, the records match row-for-row:
  - `claim_id`
  - `emp`
  - `unit`
  - `category`
  - `description`
  - `amount`
  - `date`
  - `status`
  - `pay_type`
  - `submitted_by`
  - `created_by`
  - `created_at`
  - `expense_month`
  - `charged_to`
  - `hp_unit_id`

Matching claim timeline:
- Earliest `created_at`: `2026-03-29T12:03:37.632857+00:00`
- Latest `created_at`: `2026-04-09T12:24:36.789063+00:00`

Matching claim id range:
- Earliest rows start with:
  - `MGR-2026-03-00004`
  - `MGR-2026-03-00003`
  - `MGR-2026-03-00005`
- Latest rows end with:
  - `MGR-2026-04-00035`
  - `MGR-2026-04-00036`

## Reservations And Units Audit
The later CLI-backed comparison showed the two projects are not perfectly mirrored outside `profiles` and `claims`.

Current counts observed from the two Supabase projects:
- `reservations`: 4380 vs 4378
- `units`: 47 vs 33
- `sync_logs`: 5063 vs 5
- `error_logs`: 46 vs 2
- `unit_config`: 0 vs 2
- `unit_types`: 0 vs 1

Reservation comparison:
- Shared reservation codes: 4378
- Reservation codes only present in one project: 2
  - `172HZ4R1D`
  - `HHK6AAH5N`
- Shared reservation business fields line up on:
  - `code`
  - `unit_name`
  - `start_date`
  - `end_date`
  - `guest_name`
  - `nights`
  - `booking_status`
  - `booking_type`
- The shared reservation rows diverge most often on:
  - `hp_unit_id`
  - `extra_guest`
  - `rental`
  - `total_charges`

Unit comparison:
- Shared unit IDs: 33
- Unit IDs only present in one project: 14
- Shared unit rows matched on all checked business fields except one row that differed in `property_short`.

Interpretation:
- `profiles` and `claims` still look mirrored for the audited fields.
- `reservations` and `units` are not fully mirrored, so the issue is broader than just user/expense rows.
- The different `unit_config`, `unit_types`, and log counts suggest the two projects are carrying different operational state, not just duplicated content.

## Schema Observation
- The live `claims` table is on an older schema than test.
- Live is missing some newer columns such as:
  - `invoice_number`
  - `merchant_name`
  - `ai_raw`
  - `ai_confidence`
  - `source_type`
  - `external_id`
  - `external_source`

This means:
- The common business rows are currently mirrored across both environments.
- Newer OCR/import metadata fields are not available in live yet.

## Conclusion
- This is not a case where only one side has extra user/expense rows.
- As of this audit, test and live contain the same 4 profile rows and the same 31 claim rows for the common audited fields.
- Based on deployment history, the likely operational issue was environment misrouting, where live traffic could hit the wrong Supabase project during the misconfigured period.
- From database contents alone, directionality cannot be proven with certainty because the two datasets are already mirrored.
- Outside `profiles` and `claims`, the projects are not perfectly mirrored, so no cleanup delete should happen without a row-by-row source-of-truth decision.

## Next Safe Step
- Build a cleanup plan by identifying which claims should exist only in live, which should exist only in test, and which should remain in both.
- Do not delete or rewrite rows until that business decision is approved.
